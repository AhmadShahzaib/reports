export enum DefectsType {
    MAJOR = "major",
    MINOR = "minor"
  }
  export enum DefectsCategory{
  VEHICLE="vehicle",
  TRAILER="trailer"
  }
  export enum InspectionType {
    PRE_TRIP = "pretrip",
    POST_TRIP = "posttrip",
    INCIDENCE ="incidence"
  }